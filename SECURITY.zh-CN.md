# 安全策略

[English](SECURITY.md) | [简体中文](SECURITY.zh-CN.md)

## 支持范围

当前尚未发布独立长期维护版本线。安全修复以维护中的当前代码为准；部署方应在生产发布前审查变更、按发布流程创建恢复点，并在隔离环境验证。

## 报告漏洞

请勿在公开 Issue、Pull Request、提交信息或日志中披露利用细节、真实凭据、会话标识、业务数据、可访问生产地址、含敏感信息的截图或 PoC。

仓库当前未公布独立安全邮箱或响应时限。请通过与维护者已约定的私下渠道报告，并提供：

- 受影响的提交、发布标签或部署路径；
- 最小复现步骤与前置条件；
- 影响范围与建议缓解措施；
- 已脱敏的日志、请求或截图。

维护者应先确认范围、协调修复和验证，再协商披露时间。修复或缓解措施可用前不要公开细节。

## 部署方责任

- 使用独立强密钥和项目隔离的数据目录。
- 不要将 PostgreSQL、RustFS 或 API 映射到宿主机端口。
- 仅向可信来源开放 Web 入口。
- `HTTP IP:15105` 是明文传输模式，仅适用于已接受风险的少量可信用户。
- 发布或其他计划内高风险操作前，将 PostgreSQL 与 RustFS 一并备份为同一恢复组；在独立空环境测试恢复。

当前运维限制见[生产 Compose 部署](docs/production-deployment.md)。
