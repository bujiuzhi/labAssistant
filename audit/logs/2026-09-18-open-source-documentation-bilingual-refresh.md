# 2026-09-18 公开仓库双语文档整理

记录时间：2026-09-18 10:56:18，Asia/Shanghai
仓库：`labAssistant`
分支：`dev`

## 决策与影响

- 将根 README 重构为面向公开仓库的英文入口，并新增等价的简体中文 README；设计、运维、部署、贡献和安全说明均提供中英文入口或对应版本。
- 文档索引明确源码、Flyway 迁移、Compose 清单和 OpenAPI 优先于叙述性文档；`audit/` 仅保存历史决策与验证证据。
- 将 `backup` 明确为会停止写入且需要手动恢复服务的独立恢复点，不再描述为日常在线备份；普通发布升级使用 `build` 后的 `upgrade`，后者自带恢复组。
- 本次仅整理文档与链接；未修改 API、数据库迁移、Compose 配置、镜像、生产数据或运行服务。
- 仓库未新增许可证。公开可见不等于授予复制、分发或再利用权利；许可证需由权利人另行决定。

## 验证

- 对本次修改执行 `git diff --check`，确认无空白错误。
- 扫描 Markdown 相对链接，确认本次文档引用的本地文件存在。
- 未运行前后端测试、构建、Compose 或部署：本次无代码与运行配置变更，这些检查不适用。

## 恢复与边界

- 如需撤销，只回退本次文档提交；不涉及数据、镜像、容器、卷或服务器状态恢复。
- 发布分支合并和远程推送由本次任务后续操作单独验证；本文不将其预先记为已完成。
