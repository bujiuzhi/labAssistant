# 项目文档列表与电子实验记录本附件修复

- 操作时间：2026-09-14 14:41:09（Asia/Shanghai）
- 仓库与分支：`labAssistant` / `dev`
- 代码基线：`a93ffcc` 之后的工作区修复
- 变更对象：项目文档列表、电子实验记录本过程图片与结果附件入口

## 决策与影响

项目文档上传已将正文写入 RustFS 并成功保存元数据，但列表 SQL 漏选了 `project_document.file`。该字段是 `ProjectDocument` 记录的一部分，MyBatis 在映射列表结果时因此抛出字段数量不匹配异常，表现为上传后加载失败。本次在列表查询中补充该字段，不变更数据库结构、既有文档元数据或 RustFS 对象。

电子实验记录本的附件上传接口要求附件归属一个已保存的实验记录。此前创建中的草稿仍显示上传控件，用户选择文件后才收到“请先创建实验计划”的提示。本次仅在实验计划保存后显示上传控件；草稿状态明确提示需先保存。后端对无实验归属的保护仍保留，以避免孤立对象。

## 验证

- `pnpm --dir frontend test -- --test-name-pattern='未保存的实验草稿不提供附件选择控件|文档列表和上传表单不再包含关联内容'`：22 项通过。
- `docker build --progress=plain --target build --file infra/Dockerfile.api .`：后端 Maven `verify` 通过，74 项测试通过；新增 SQL Provider 回归测试包含对象存储字段。
- `git diff --check`：通过。

未以真实用户会话重复上传业务文件；本次不清理、迁移或重置现有业务数据。后续本机生产 Compose 升级应生成新的数据库和对象存储备份，可回滚到升级前发布镜像；本次代码不含 Flyway 迁移。
