# 电子实验记录本列表滚动与拖拽边界变更记录

- 变更时间：2026-08-04 15:39:41
- 变更范围：电子实验记录本左侧实验列表
- 数据影响：无数据库、接口或业务数据变更

## 变更内容

- 隐藏左侧实验列表的可见滚动条，保留鼠标滚轮、触控板、触屏及键盘滚动能力。
- 删除独立的 6px 拖拽网格列，将拖拽热区并入左侧列表卡片右边缘。
- 拖拽边界默认沿用卡片边框，悬停、键盘聚焦和拖拽时显示蓝色边线与阴影。
- 拖拽宽度继续限制在 390px 至 630px，并新增方向键、Home、End 键盘调整能力。
- 新增 `frontend/tests/eln-layout.test.ts`，覆盖滚动条隐藏、内嵌边界和蓝色悬停反馈。

## 验证记录

- 远程环境前端自动化测试：13 项全部通过。
- 远程环境 `vue-tsc -b && vite build`：通过；仅保留既有大分包体积提示。
- 浏览器自动验收因安全校验服务暂时不可用而停止，未绕过安全校验。

## 视觉微调

- 拖拽边线改为强调色与白色混合后的浅蓝色。
- 外圈透明度由 12% 降至 8%，发光透明度由 42% 降至 22%，发光半径由 10px 收敛至 8px。
