# 2026-09-14 普通用户逻辑删除

- 时间：2026-09-14 14:30:00（Asia/Shanghai）
- 仓库与分支：`/Users/bujiu/work/code/labAssistant`，`dev`
- 对象：一账号一组织模型下的组织普通用户删除能力；不包含组织归档、组织管理员交接或跨组织成员模型。

## 决策与影响

普通用户删除采用逻辑删除而非物理删除。`V3__logical_user_deletion.sql` 新增删除元数据和状态一致性约束；删除服务撤销角色与目标签发的未使用邀请码，轮换不可知密码哈希，匿名化账号资料，并写入最小必要的身份审计记录。

为避免活跃资源失去负责人，仍负责未归档项目或未完成实验的账号会被拒绝删除，须先在业务功能中交接负责人。项目成员、实验参与人及既有审计外键保留，但查询列表过滤已删除账号。删除后会话版本递增，下一次请求会因账户不再有效而失效。

## 兼容、恢复与验证

迁移会将早期异常状态收敛为既有 `is_active` 所表达的状态，再建立状态一致性约束；不会删除业务数据。逻辑删除不可通过管理界面恢复；如误删，应根据部署前备份按既有恢复流程恢复，并评估恢复期间新增数据。

已执行 `docker build --progress=plain --target build --file infra/Dockerfile.api .`：项目固定 Java 25 环境完成编译、73 个测试和打包，结果为 `BUILD SUCCESS`。已执行 `pnpm run build && pnpm test`：前端类型检查、生产构建和 21 个 Node 测试通过。已执行 `ruby -e 'require "yaml"; YAML.load_file("contracts/openapi.yaml")'` 与 `git diff --check`：接口契约 YAML 可解析，补丁未发现空白错误。未执行部署、数据库迁移或运行中服务重启。

本记录不包含账号、邮箱、密码、Cookie 或真实业务数据，也不代表已重启或部署任何环境。
