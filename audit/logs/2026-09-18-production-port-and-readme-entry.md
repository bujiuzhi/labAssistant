# 2026-09-18 生产默认端口与 README 部署入口调整

记录时间：2026-09-18 11:02:19，Asia/Shanghai
仓库：`labAssistant`
分支：`dev`

## 决策与影响

- 生产 Compose、环境模板、Nginx 运行时默认值和生产脚本的默认公网端口由 `15105` 调整为 `13501`；Web 仍是唯一映射到宿主机的服务。
- 中英文 README 将生产部署移到架构说明之后，提供空库首次安装的最短命令；本地开发说明保留在后续章节。
- 已存在的服务器 `.env.production` 显式设置端口时不会被本次默认值覆盖。若希望服务器改用 `13501`，应在受控升级窗口修改两个端口变量、调整防火墙或安全组，再按生产部署指南升级并验收。
- 本次未访问、重启或修改任何本机或远程 Compose 服务、容器、镜像、卷、数据库或 RustFS 数据。

## 验证

- 扫描非历史资产中的 `15105` 引用，确认生产默认值、配置模板、部署文档、README 与脚本测试均已同步为 `13501`。
- 执行 `node --test scripts/tests/production.test.mjs`，44 项隔离测试通过，未调用真实 Docker 变更服务。
- 使用命令级非敏感测试变量执行生产 Compose `config --quiet`，静态配置校验通过；未创建或启动服务。
- 文档与配置检查执行 `git diff --check`。

## 恢复与边界

- 如需恢复默认端口，只回退本次提交；运行中的服务不会自行改变端口。
- 端口变更后的目标服务器网络、访问和业务验收属于后续部署操作，不能由本次静态检查替代。
