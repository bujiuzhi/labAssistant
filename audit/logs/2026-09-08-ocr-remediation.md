# OCR 高风险问题修复记录

## 基线与范围

- 时间：2026-09-08 15:05:00，Asia/Shanghai。
- 仓库：`labAssistant`；分支：`dev`。
- 输入：Open Code Review 使用已配置 LLM 对工作区扫描后返回的部分结果。
- 本次仅修复经源码复核确认的严重、高风险问题及直接相关校验；未提交、推送、部署或操作正式数据。

## 决策与影响

- 登录成功后轮换既有 Session ID；无会话注销不再触发空指针。
- 登录名按当前无组织字段的登录协议改为全局唯一。新增 V3 迁移先检测跨组织重名，发现冲突时明确终止，不自动改名。
- 实验查询纳入实验负责人和参与人；项目关联写入对所有角色校验当前组织。
- 实验创建保存计划时间和参与人；PATCH 省略字段时保留计划时间及 ELN，只对明确提交的字段执行替换。
- 新增实验复制、附件上传、删除和正文读取；附件正文以 BYTEA 保存，结果文件下载响应使用 attachment，过程图片仅允许 JPG/PNG 且响应启用 `nosniff`。
- 统一应用 multipart 上限为 25 MiB 文件、27 MiB 请求，生产 Nginx 同步使用 27 MiB 请求上限。
- 关闭 Spring Boot Flyway 自动迁移，由项目现有初始化器作为唯一执行入口，避免一次启动重复调用。
- 就绪探针对数据库结果缓存 5 秒，限制公开高频调用对连接池的占用。
- 参数绑定错误返回固定安全说明；认证只捕获认证异常，不再把数据库等系统故障伪装为 401。

## 数据迁移与上线前检查

- 新增 `V3__experiment_attachments_and_global_username.sql`，不改写 V1/V2。
- V3 新建全局用户名唯一索引、实验附件正文表及实验参与人/附件查询索引。
- 上线前必须先执行重复用户名只读预检；如果 V3 因重名失败，应由业务负责人确认重命名映射后新增补偿迁移，禁止直接删除账号或数据。
- 附件正文进入 PostgreSQL 备份范围；容量、保留周期和恢复时间尚未在正式数据规模下验证。

## 验证

- Java 25 生产 Dockerfile 构建阶段执行 `mvn verify`：44 项测试通过，JAR 打包成功。
- 针对会话、实验 PATCH、组织范围、参与人、附件和就绪缓存的 15 项测试通过。
- 一次性 PostgreSQL 18.4 空库按顺序执行 V1、V2、V3：通过；正文表、全局唯一索引和表注释可查询。
- 第二个一次性 PostgreSQL 18.4 数据库构造跨组织重名账号后执行 V3：迁移按设计中止并返回明确错误，未自动修改账号。
- `node --test scripts/tests/production.test.mjs`：24 项通过。
- `pnpm --dir frontend test`：16 项通过。
- `pnpm --dir frontend run build`：通过；保留既有大 chunk 警告。
- OpenAPI YAML 解析及 `git diff --check`：通过。

宿主机使用 JDK 26 执行全量测试时，既有 PDF 夹具测试阶段 JVM 以 134 退出；同一完整测试集已在正式 Java 25 构建镜像中通过，因此以 Java 25 结果为项目基线。

修复后使用已配置的 `xty/claude-fable-5` 对 12 个相关后端文件执行非委托 OCR 复审；
运行 14 分 56 秒后仍有 9 个文件未完成，人工终止，因此状态为失败而非“复审通过”。已返回的 6 条部分意见中，
3 条中风险（计时回绕、探针锁等待、multipart 错误映射）和 2 条明确低风险（字段校验提示、正文长度）已修复；
剩余 1 条低风险是 BYTEA 附件并发读取内存压力，保留为容量与并发验收事项，不在没有基准的情况下引入新的流式存储依赖。

## 恢复要点与未执行项

- 应用回退时，新表和唯一索引可保留；旧版本不会读取附件正文表。不要通过删除表或索引实施常规回退。
- 已写入的新附件如需回退业务行为，应先停止写入、保留数据库备份，再选择向前修复或经批准恢复备份。
- 未执行真实服务器发布、生产迁移、生产备份恢复、真实浏览器登录或附件往返验收。

## 二次复审问题修复

- 时间：2026-09-08 17:34:27，Asia/Shanghai。
- 第二次非委托 OCR 对 32 个受支持的变更文件执行审查，因长时间未返回终态而人工中止；结果状态为失败，但落盘了 10 条部分意见。源码复核确认 5 条中风险和 1 条低风险，排除迁移表白名单、镜像健康检查、PostgreSQL 保留 schema 前缀及验收目录约束 4 条误报。
- 就绪缓存到期后，数据库探测进行中的并发请求直接按未就绪处理，不再复用过期成功状态或堆积等待。
- 生产路径必须位于当前执行用户的 `~/work/data/labAssistant` 或 `~/work/code/labAssistant/data`，不再接受仅后缀相同的任意绝对路径。
- 预检统一校验三个 Secret 的普通文件类型和 `0444` 权限，并按应用规则校验管理员密码长度、字符类别、控制字符及可选单行尾。
- `secrets` 在同级 `0700` 暂存目录完成全部生成后再原子发布；失败时不暴露部分最终目录，保留私有暂存证据以供人工处置。
- 生产 profile 的数据库 URL 和用户名改为必填变量；正式 Compose 继续显式提供二者。
- direct TLS 增加独立宿主绑定端口；隔离验收使用 Docker 动态端口并在容器启动后读取实际映射，消除“探测后释放”竞态。
- Java 25 Docker 构建阶段执行 `mvn verify`：45 项测试通过，JAR 打包成功。
- `bash -n`、验收脚本 Node 语法检查、部署脚本 28 项测试、direct TLS 双 Compose `config --quiet` 及 `git diff --check`：通过。
- 本轮仍未执行真实生产部署、真实数据迁移、生产重启或完整隔离 Compose 业务验收。
