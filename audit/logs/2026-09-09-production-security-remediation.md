# 生产安全问题修复记录

## 范围与决策

- 时间：2026-09-09 10:36:05，Asia/Shanghai。
- 仓库：`labAssistant`；分支：`dev`。
- 范围：修复最近代码与部署审查确认的生产登录、项目文档内联和前端外联风险；不提交、不推送、不连接服务器、不迁移或修改正式数据。
- 登录页移除开发账号提示；管理员创建和重置用户必须提供至少 16 个字符、包含大小写字母/数字/符号且不含空白的密码。生产首次管理员仍只从 Secret 文件读取。
- HTTP Basic 关闭。应用对同一用户名连续 5 次认证失败实施 15 分钟有界内存限制；成功认证清除记录。单实例限制不替代多副本共享限流，横向扩容前须改为共享实现。
- 项目文档忽略客户端 MIME，使用扩展名白名单和基础文件头判定。HTML、SVG 和未知类型被拒绝；Office、文本及历史异常 MIME 仅作附件下载，只有已验证 PDF/位图图片可内联，所有正文响应加 `nosniff`、私有不缓存与最小 CSP。
- 前端改用本地 Iconify 集合并在 Nginx 添加同源 CSP；锁文件经 pnpm 工作区 override 固定 `nanoid` 为 `3.3.18`、`postcss` 为 `8.5.23`，消除生产依赖审计项。

## 验证与恢复

- `pnpm audit --prod --audit-level high`：未发现已知漏洞。
- 前端 `pnpm test`：18 项通过；`pnpm build`：通过，保留既有大 chunk 警告。
- Java 25 Maven 容器 `mvn verify`：51 项通过并完成 JAR 打包；包含新增密码策略、登录限流和文档文件头校验单元测试。
- Nginx 通过正式 entrypoint 生成配置后执行 `nginx -t`：通过。
- 恢复时不删除已上传文档或数据库记录。旧版本会失去新登录保护和文档响应边界；若必须回退，保留备份并优先向前修复。
