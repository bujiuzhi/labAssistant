# 项目文档与当前实现对齐

_文档变更审计；时间基准：Asia/Shanghai。_

---

## 📋 基线与范围

- 记录时间：2026-09-07 18:00:36，语义为本次文档重写过程中的基线记录时间
- 仓库：labAssistant，分支：dev，代码基线：9fe3795
- 开始检查时工作区干净；未重新获取远端引用，不声明远端当前同步
- 对象：根/模块 README、docs 三份正式规范与索引、OpenAPI、审计入口和历史证据索引
- 用户请求为重写文档；本次不改变业务实现、配置值、数据库、服务器或历史来源内容

## ✍️ 决策与影响

沿用三个正式文档的职责分工，由 README 导航；完整 HTTP 契约统一维护于 OpenAPI，
物理结构以 Flyway 为唯一来源。以当前源码校正文档，不以旧设计或历史截图推定实现。
使用 Markdown/Mermaid 文档技能统一结构，架构源保留在概要设计。

主要纠偏：

| 原说明 | 当前核验结果 | 文档影响 |
| --- | --- | --- |
| 文档存于 RustFS，支持 LibreOffice 转换 | 文档正文为 PostgreSQL BYTEA，preview 原样返回 | 重写架构、存储和备份说明 |
| 改 prod 参数即可部署 | Compose 固定 dev，部分变量未传入容器 | 增加配置生效表与生产准备门槛 |
| 空库已有生产管理员/强制首次改密 | 无独立生产身份初始化；无强制首次改密 | 明确空生产库不可直接登录 |
| 已完成实验可补充修订 | 后端 completed 不可编辑 | 统一状态说明 |
| 成员、参与人、复制/附件 API 全部完成 | 部分字段未落库；页面复制为新增草稿；附件及专用 copy 无后端 | 明确客户端与 API 边界 |
| 后端创建幂等和完整审计 | 幂等表未使用，常规写入审计未全覆盖 | 删除过度保证 |
| 所有表已有中文 COMMENT、完整约束 | V1/V2 未定义 COMMENT，部分约束未建立 | 按实际结构说明并登记历史缺口 |
| ready 覆盖全部依赖 | 仅检查 JDBC 连接 | 分离进程、数据库、业务验收 |

OpenAPI 调整是对已有实现的描述修正；删除未实现操作不表示删除了后端功能。
旧客户端若依赖历史契约中的能力，需按当前实现重新核验；没有变更实际 HTTP 协议。

## ✅ 验证与缺口

已执行本地文档检查：10 份 Markdown、107 个相对链接/锚点、9 段 Shell 示例语法和标题/围栏结构通过。
OpenAPI 最终修订后复查通过：24 个路径、31 个操作与 Controller 逐项一致，146 处内部引用
（47 个唯一目标）全部解析；无重复 YAML 键和 operationId，路径参数、安全方案及 11 个响应模型字段核对通过。
`git diff --check` 通过。

架构图使用临时 Mermaid 11.15.0 在隔离无头 Chrome 中解析和渲染，两个主题均为
6 个节点；禁用浏览器网络并拦截全部请求，实际外连请求为 0。明暗主题截图已逐张检查，
文字与连线可读，没有遮挡。等待字体与绘制帧后复查深色主题，未改变图源。

最初加载外部 Mermaid 的浏览器方案被自动审批拒绝，原因是外部脚本可能带来项目内容外传风险。
后改为只下载公开渲染库到临时目录、浏览器完全离线加载本地库；该方案获准并完成验证。
临时渲染库和截图不作为项目依赖或正式图源，不进入 Git；正式源保留于概要设计。

两位只读复核代理交叉检查源码与文档，已修正当前组织接口接入程度、RustFS 目录所有者变更、
独立 Vite 监听地址、用户选项下级组织范围、项目 ended 筛选以及实验关联项目组织校验的说明。
本次为文档修改，不运行连接数据库的集成测试，也不将已有测试文件或历史报告视为本次通过。

已确认宿主机默认 Maven 使用 JDK 21，低于项目 Java 25 要求；
本次未据此运行后端构建。全量业务测试、服务运行、新服务器部署和浏览器业务验收未执行。

## 🔐 风险与恢复

源码复核还发现：超级管理员创建/调整实验项目关系时缺少关联项目组织校验，
可形成跨组织关联；已写入详细设计、契约和生产部署门槛。
上线前需按运维指南处理生产初始化、文件安全、会话/组织边界及目标业务缺口。
本次仅文档纠偏，未解决这些代码问题。

原始来源、来源哈希、旧审计日志和历史视觉 QA 保留。
恢复本次变更只需审查后撤销对应文档差异，不涉及数据库回滚、服务重启或数据恢复。
未执行提交、推送或发布。
