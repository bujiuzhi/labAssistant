# 2026-09-17 项目文档与本地构建产物整理

记录时间：2026-09-17 11:19:57（Asia/Shanghai）
仓库与分支：`labAssistant` / `dev`；未提交工作区整理，基线为 `b6e9f1a`

## 决策与影响

- 根目录 `README.md` 按常见开源项目入口组织为范围、功能、架构、前置条件、开发快速开始、配置、测试、生产部署、文档、贡献、安全、许可证与目录说明；不复制详细设计与运维细节。
- `docs/README.md` 收敛为正式文档地图、事实来源与维护规则；设计、部署和运维正文继续保留为当前有效文档。
- 新增 `CONTRIBUTING.md`，记录本地准备、提交边界、必要检查与 Pull Request 信息要求；新增 `SECURITY.md`，明确当前未公开独立安全邮箱或响应时限时的私下报告原则与部署方责任。
- 未新增 `LICENSE` 或虚构 CI 徽章、发布版本、开源许可证、安全联系地址；README 明确仓库目前未声明开源许可证。
- 删除未被仓库引用、包含已失效临时截图路径的 `design-qa.md`。
- 删除 `frontend/public/prototype/` 下两份与 `frontend/src/assets/prototype/` 字节相同且未被引用的 PNG 素材。
- 删除本地可再生成且已被 `.gitignore` 忽略的 `frontend/node_modules`、`frontend/dist` 与 `backend/target`。未删除 `data/`，其中可能包含当前本地 Compose 服务所需密钥或持久化数据。

历史审计记录、需求来源、Flyway 迁移、部署配置、接口契约及已提交源码均保留。上述操作未部署、重启、迁移或删除任何服务器数据。

## 验证

检查删除目标没有源码引用，确认重复 PNG 的 SHA-256 与 `src/assets` 副本一致；已执行 Markdown 内部链接与差异格式检查。构建产物已删除，因此本记录不将此前构建结果表述为本次构建验收；后续需要时按锁文件重新安装依赖并执行对应测试。
