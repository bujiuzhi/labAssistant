# 2026-09-11 租户身份边界修复

- 操作时间：2026-09-11 14:13:08，Asia/Shanghai
- 仓库与分支：`labAssistant`，`dev`
- 代码基线：`860a443 fix: 放宽统一密码规则`
- 本次对象：身份用户选项查询、普通用户角色分配、相关单元测试和正式设计文档

## 决策与影响

- 将 `/api/v1/auth/users/options` 收敛为仅返回当前组织有效用户，移除基于组织层级的递归查询，避免在未来存在组织层级数据时向当前组织泄露子组织账户信息。
- 用户管理的可分配角色选项和服务端写入共同排除 `super_admin`。超级管理员仍仅能由 bootstrap 或平台组织开通流程建立，并以账号特权标记授予组织内通配权限。
- 本次不改变现有超级管理员账号、普通角色权限码、接口路径、数据库结构或生产部署参数。

## 验证与恢复

- 已执行：使用项目固定的 `maven:3.9.12-eclipse-temurin-25` 构建阶段完成 `mvn --batch-mode --no-transfer-progress verify`；67 个后端测试通过（0 failures、0 errors、0 skipped），包含身份、会话、组织、项目和实验授权路径；`git diff --check` 无输出；已人工复核本次差异。
- 环境说明：宿主 JDK 为 21，不能编译项目要求的 Java 25；宿主 Maven 测试在编译阶段以“不支持发行版本 25”终止，未将其计为用例失败。
- 未执行：目标服务器部署、生产数据库迁移和真实多组织端到端验收。
- 本次不写入数据库；如需撤销，回退本次代码和文档差异即可。
