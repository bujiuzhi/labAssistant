# OCR 发布阻断问题修复

- 操作时间：2026-09-14 18:00:46（Asia/Shanghai）
- 仓库与分支：`labAssistant` / `dev`
- 审查范围：`origin/dev..HEAD` 的待推送变更，以及针对审查结论的本次工作区修复。

## 决策与影响

OCR 发现已发布的 Flyway V1 被改写会阻断首版数据库升级。已将 V1 恢复为远端发布内容，并将账号全局唯一、平台标记、会话版本、邀请码及历史 BYTEA 兼容表移动到 V2；V4 新增平台账号与平台组织的复合外键，拒绝租户账号获得平台标记。逻辑删除迁移补齐既有 `deleted` 行的删除时间后再建立约束。

同时修复平台控制面服务与前端路由的双重平台边界、开发种子账号的冲突标记、服务端 PDF 回退渲染、密码和邀请码关闭后的内存清理、工作台 OpenAPI 契约、对象存储禁用场景的就绪检查、IPv4 入口校验以及 Bootstrap 密码 Unicode 长度校验。

本次不执行数据库迁移、生产重启或远程推送；本机生产环境保留原状态。由于 V1 已恢复，使用此前本机预发布 V1 校验和的环境需要按发布说明走隔离备份与受控迁移验证，不能直接以旧镜像状态替代正式首版升级验证。

## 验证与恢复

- `pnpm --dir frontend test`：24 项通过。
- `pnpm --dir frontend build`：通过。
- `docker build --progress=plain --target build --file infra/Dockerfile.api .`：Java 25 环境中 77 项后端测试通过。
- 后续推送前需完成当前修复差异的 OCR 复审；若发现中高风险问题，继续修复而不推送。

本次结构修复可通过回退该修复提交恢复代码，但不得再次改写已发布 V1；任何数据库恢复须先使用生产脚本创建恢复组并在隔离环境验证。
