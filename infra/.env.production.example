# 复制为仓库根 .env.production，由 bash scripts/deploy.sh 使用。
# 默认值可直接使用；只修改需要调整的项目，不加引号或行内注释。
APP_PORT=13501
ADMIN_USERNAME=admin
ADMIN_DISPLAY_NAME=平台管理员

# 可选：固定允许访问的 IPv4。默认 auto，无需填写公网 IP，使用请求中的 IPv4 与端口。
# PUBLIC_HOST=auto
# 路径、Git 发布版本和缺失密钥自动处理；旧版完整配置继续兼容，请勿覆盖已有部署配置。
