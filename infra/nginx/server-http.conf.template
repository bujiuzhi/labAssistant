# 经明确授权的公网明文入口。仅 Web 映射宿主端口，内部服务不暴露。
server {
    listen 8080 default_server;
    server_name ${MATERIALS_LAB_PUBLIC_HOST};
    if ($host != "${MATERIALS_LAB_PUBLIC_HOST}") {
        return 421;
    }
    include /tmp/nginx/locations.conf;
}
